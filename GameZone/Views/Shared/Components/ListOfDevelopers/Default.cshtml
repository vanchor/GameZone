@model IEnumerable<GameZone.Domain.Core.Entities.Company>

<div class="d-flex align-items-center">
    <button class="btn btn-outline-secondary rounded-circle me-3" onclick="ReloadDevelopersList()">
        <i class="bi bi-arrow-clockwise fs-4"></i>
    </button>
    
    <input class="form-control" type="text" id="searchName" 
            onkeyup="findByNameInList('searchName', 'companyList')" 
            placeholder="Search by name..." title="Type in a name">
</div>
<br />

<div class="list-group" id="companyList">
    @foreach (var company in Model)
    {
        var itemId = "ListItem" + @company.Id;
        <a id="@itemId" class="list-group-item list-group-item-action  p-2 developerItem">
            <p hidden>@company.Id</p>
            <img src="~/images/Media/Companies/@company.LogoUrl" alt="Logo" width="40px" class="rounded" />
            <span class="align-middle">@company.Name</span>
        </a>
    }
</div>
            